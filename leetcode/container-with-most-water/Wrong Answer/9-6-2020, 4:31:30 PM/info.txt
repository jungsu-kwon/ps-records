{"id":391739502,"lang":"cpp","time":"11 months","timestamp":1599377490,"status_display":"Wrong Answer","runtime":"N/A","url":"/submissions/detail/391739502/","is_pending":"Not Pending","title":"Container With Most Water","memory":"N/A","code":"class Solution {\nprivate : \n    int searchNext(std::vector<int> &height,int startInd,bool fromLeft){\n        int nextInd = 0 ;\n        if (fromLeft){\n            auto itFound = std::upper_bound(height.begin() + startInd,height.end(),height[startInd]);\n            if (itFound == height.end()){\n                return -1;\n            }\n            nextInd = itFound - height.begin();\n        }\n        else{\n            auto itFound = std::upper_bound(height.rbegin() + height.size() - 1 - startInd,\n                                                        height.rend(),\n                                                        height[startInd]);\n            if (itFound == height.rend())\n                return -1;\n            nextInd = height.size() - 1 - (itFound - height.rbegin());\n        }\n        return nextInd ;\n\n    }\npublic:\n    int maxArea(vector<int>& height) {\n        int maxArea = 0 ;\n        int l = 0, r = height.size()-1;\n        int maxVolume =(r-l) * std::min(height[r],height[l]);\n        while (l < r){\n            if (height[l] < height[r])\n            {\n                l = searchNext(height,l,true);\n                if (l == -1)\n                    break;\n                else \n                    maxVolume = std::max(maxVolume,(r-l) * std::min(height[r],height[l]));\n            }\n            else\n            {\n                r = searchNext(height,r,false);\n                if (r == -1)\n                    break;\n                else\n                    maxVolume = std::max(maxVolume,(r-l) * std::min(height[r],height[l]));\n            }\n        }\n        return maxVolume;\n    }\n};","compare_result":"11111111111001011000001110000000000000000000000011","title_slug":"container-with-most-water"}